[package]
name = "tcp_throttle"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.0", features = ["derive"] }
libc = "0.2.153"
tokio = { version = "1.37.0", features = ["rt", "net", "macros", "io-util", "time"] }
